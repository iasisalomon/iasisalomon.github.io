---
// The navbar is identical on every page and every item is an in-page section
// link (`/#id`). On the homepage they smooth-scroll; from any other page they
// navigate home and the on-load handler scrolls into place. "Blog" points at
// the homepage's From-the-Blog teaser; the full archive (/blog) is reached from
// there and from the footer — keeping the primary nav uniform (never a hard URL
// jump to a different page).
const links = [
  { label: 'About', href: '/#about' },
  { label: 'Expertise', href: '/#expertise' },
  { label: 'Work', href: '/#work' },
  { label: 'Projects', href: '/#projects' },
  { label: 'Contact', href: '/#discussion' },
  { label: 'Blog', href: '/#blog' },
];
---

<nav class="navbar navbar-expand-lg navbar-dark fixed-top" id="mainNav">
  <div class="container">
    <a class="navbar-brand" href="/">Iasi Salomon</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav main-nav">
        {links.map((link) => (
          <li class="nav-item">
            <a class="nav-link" href={link.href}>{link.label}</a>
          </li>
        ))}
      </ul>
    </div>
  </div>
</nav>

<style>
  #mainNav {
    background-color: rgba(28, 31, 35, 0.98);
    padding: 0.6rem 0;
    transition: all 0.3s ease;
    z-index: 1030;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
  }

  .navbar-brand {
    font-weight: 700;
    letter-spacing: 0.2px;
    color: #fff;
  }

  .main-nav .nav-item {
    margin: 0 0.15rem;
  }

  .main-nav .nav-link {
    padding: 0.5rem 0.9rem;
    font-weight: 500;
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.85);
    position: relative;
    letter-spacing: 0.2px;
    transition: color 0.3s ease, background-color 0.3s ease;
    border-radius: 4px;
    background-color: transparent;
  }

  /* Animated underline */
  .main-nav .nav-link::after {
    content: '';
    position: absolute;
    bottom: 4px;
    left: 10%;
    width: 80%;
    height: 2px;
    background-color: var(--color-accent, #4d9aff);
    transform: scaleX(0);
    transform-origin: center;
    transition: transform 0.3s ease;
  }

  .main-nav .nav-link:hover {
    color: #fff;
    background-color: rgba(255, 255, 255, 0.1);
  }

  .main-nav .nav-link.active {
    color: #fff;
  }

  .main-nav .nav-link.active::after {
    transform: scaleX(1);
  }

  .navbar-toggler {
    border: none;
    background-color: rgba(255, 255, 255, 0.08);
    padding: 0.4rem 0.6rem;
    font-size: 0.9rem;
    transition: all 0.2s ease;
  }

  .navbar-toggler:hover {
    background-color: rgba(255, 255, 255, 0.15);
  }

  .navbar-toggler:focus {
    box-shadow: none;
  }

  @media (max-width: 992px) {
    #mainNav {
      padding: 0.5rem 0;
    }

    .navbar-nav {
      padding: 0.5rem 0;
      text-align: center;
      width: 100%;
    }

    .main-nav .nav-item {
      margin: 0;
      width: 100%;
    }

    .main-nav .nav-link {
      padding: 0.6rem 1rem;
      display: block;
    }

    /* Underline replaced by a subtle background highlight on mobile */
    .main-nav .nav-link::after {
      display: none;
    }

    .main-nav .nav-link.active {
      background-color: rgba(255, 255, 255, 0.15);
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const nav = document.getElementById('mainNav');
    const collapse = document.getElementById('navbarNav');
    const isHome = window.location.pathname === '/' || window.location.pathname === '';

    const anchorLinks = Array.from(
      document.querySelectorAll('#navbarNav .nav-link')
    ).filter((l) => (l.getAttribute('href') || '').includes('#'));

    function hashOf(link) {
      const href = link.getAttribute('href') || '';
      const i = href.indexOf('#');
      return i >= 0 ? href.slice(i) : null; // e.g. '#about'
    }

    function scrollToSection(hash) {
      const section = document.querySelector(hash);
      if (!section) return false;
      window.scrollTo({ top: section.offsetTop - nav.offsetHeight - 10, behavior: 'smooth' });
      return true;
    }

    function setActive(hash) {
      anchorLinks.forEach((l) => l.classList.toggle('active', hashOf(l) === hash));
    }

    if (!isHome) return; // On other pages the links are plain navigation.

    // Smooth-scroll for in-page section links.
    anchorLinks.forEach((link) => {
      link.addEventListener('click', function (e) {
        const hash = hashOf(link);
        if (!hash || !document.querySelector(hash)) return; // let it navigate
        e.preventDefault();
        setActive(hash);
        scrollToSection(hash);
        // Close the mobile menu via Bootstrap's API rather than simulating a
        // toggler click — a synthetic click fired mid-transition desyncs
        // Bootstrap's internal state and breaks the next tap on touch devices.
        if (collapse.classList.contains('show') && window.bootstrap) {
          window.bootstrap.Collapse.getOrCreateInstance(collapse).hide();
        }
      });
    });

    // Scroll-spy: highlight the section currently in view.
    let ticking = false;
    window.addEventListener('scroll', function () {
      if (ticking) return;
      ticking = true;
      setTimeout(function () {
        const scrollPos = window.scrollY + nav.offsetHeight + 20;
        let current = null;
        document.querySelectorAll('section[id]').forEach(function (sec) {
          if (scrollPos >= sec.offsetTop && scrollPos < sec.offsetTop + sec.offsetHeight) {
            current = '#' + sec.id;
          }
        });
        if (
          !current &&
          window.scrollY + window.innerHeight >= document.documentElement.scrollHeight - 5
        ) {
          const last = anchorLinks[anchorLinks.length - 1];
          if (last) current = hashOf(last);
        }
        if (current) setActive(current);
        ticking = false;
      }, 100);
    });

    // Arriving with a hash (e.g. from another page): scroll into place.
    if (window.location.hash) {
      const hash = window.location.hash;
      setTimeout(function () {
        scrollToSection(hash);
        setActive(hash);
      }, 300);
    }
  });
</script>
